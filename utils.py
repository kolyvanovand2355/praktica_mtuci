import os
import json
import torch
import random
import numpy as np

CLASS_NAMES = [
    'Ограничение 20 км/ч',              # 0
    'Ограничение 30 км/ч',              # 1
    'Ограничение 50 км/ч',              # 2
    'Ограничение 60 км/ч',              # 3
    'Ограничение 70 км/ч',              # 4
    'Ограничение 80 км/ч',              # 5
    'Конец ограничения 80 км/ч',        # 6
    'Ограничение 100 км/ч',             # 7
    'Ограничение 120 км/ч',             # 8
    'Обгон запрещён',                   # 9
    'Обгон грузовикам запрещён',        # 10
    'Перекрёсток со второстепенной',    # 11
    'Главная дорога',                   # 12
    'Уступи дорогу',                    # 13
    'Стоп',                             # 14
    'Движение запрещено',               # 15
    'Грузовикам запрещено',             # 16
    'Въезд запрещён (кирпич)',          # 17
    'Прочая опасность',                 # 18
    'Опасный поворот налево',           # 19
    'Опасный поворот направо',          # 20
    'Несколько поворотов',              # 21
    'Неровная дорога',                  # 22
    'Скользкая дорога',                 # 23
    'Сужение дороги справа',            # 24
    'Дорожные работы',                  # 25
    'Светофорное регулирование',        # 26
    'Пешеходный переход',               # 27
    'Дети',                             # 28
    'Велосипедная дорожка',             # 29
    'Гололёд',                          # 30
    'Дикие животные',                   # 31
    'Конец всех ограничений',           # 32
    'Движение направо',                 # 33
    'Движение налево',                  # 34
    'Движение прямо',                   # 35
    'Движение прямо или направо',       # 36
    'Движение прямо или налево',        # 37
    'Объезд препятствия справа',        # 38
    'Объезд препятствия слева',         # 39
    'Круговое движение',                # 40
    'Конец зоны запрета обгона',        # 41
    'Конец запрета обгона грузовиками'  # 42
]

NUM_CLASSES = 43
IMG_SIZE = 64
DEVICE = torch.device('cuda' if torch.cuda.is_available() else 'cpu')

def set_seed(seed=42):
    """Фиксируем случайность для воспроизводимости"""
    random.seed(seed)
    np.random.seed(seed)
    torch.manual_seed(seed)
    if torch.cuda.is_available():
        torch.cuda.manual_seed_all(seed)

def save_json(data, path):
    """Сохраняем результаты в JSON"""
    os.makedirs(os.path.dirname(path), exist_ok=True)
    with open(path, 'w', encoding='utf-8') as f:
        json.dump(data, f, ensure_ascii=False, indent=2)

def load_json(path):
    with open(path, 'r', encoding='utf-8') as f:
        return json.load(f)

def count_parameters(model):
    """Считаем количество параметров модели"""
    return sum(p.numel() for p in model.parameters() if p.requires_grad)